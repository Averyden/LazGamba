{
  "devDependencies": {
    "esbuild": "^0.25.6",
    "typescript": "^5.6.3"
  },
  "scripts": {
    "build": "esbuild src/scripts/main.ts --bundle --outdir=./src/scripts/compiledJS --format=esm",
    "watch": "esbuild src/scripts/main.ts --bundle --outdir=./src/scripts/compiledJS --format=esm --watch"
  }
}
